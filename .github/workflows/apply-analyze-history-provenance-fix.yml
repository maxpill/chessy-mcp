name: Apply analyze history provenance fix

on:
  push:
    branches:
      - fix/chess-mcp-ultra-audit-2026-08-30

permissions:
  contents: write

jobs:
  apply:
    if: ${{ !contains(github.event.head_commit.message, '[analyze-history-fix-applied]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Apply fix
        run: python scripts/apply_analyze_history_provenance_fix.py
      - name: Compile
        run: python -m compileall -q mcp_server/server.py
      - name: Commit fix
        shell: bash
        run: |
          if git diff --quiet -- mcp_server/server.py; then
            echo "No source change required"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add mcp_server/server.py
          git commit -m "fix: preserve complete PGN history through game analysis [analyze-history-fix-applied]"
          git push origin HEAD:${GITHUB_REF_NAME}
